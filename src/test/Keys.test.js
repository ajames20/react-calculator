import React from 'react';
import ReactDOM from 'react-dom';
import Keys from '../components/Keys';

it('renders Keys Component', () => {
  const div = document.createElement('div');
  ReactDOM.render(<Keys />, div);
});
